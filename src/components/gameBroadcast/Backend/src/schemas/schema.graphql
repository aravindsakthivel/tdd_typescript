type User @key(fields: "firstName") {
  firstName: String
  lastName: String
}

extend type Query {
  users: [User]
}

# extend type Mutation {
#   addUser(firstName: String, lastName: String): User
# }
